services:
  app:
    build: .
    image: tissuepro-be
    container_name: tissuepro-be
    ports:
      - "${PORT:-5176}:${PORT:-5176}"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
